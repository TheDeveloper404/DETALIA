# === Dependențe ===
node_modules/
.pnpm-store/

# === Build & output Next.js ===
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# === Variabile de mediu / secrete (NICIODATĂ în git) ===
.env
.env.local
.env.*.local
.env.development
.env.production
# .env.example SE comite (template fără valori reale) — vezi mai jos că NU e ignorat

# === Vercel ===
.vercel/

# === Loguri & temporare ===
*.log
npm-debug.log*
pnpm-debug.log*
.tmp-*
*.tmp

# === Teste ===
coverage/
playwright-report/
test-results/
playwright/.cache/
.playwright-mcp/
.nyc_output/
# Screenshot-uri de verificare locală (Playwright/manual) — NU se comit
hero-b-fixed.png

# === Lint / cache build ===
.eslintcache
.turbo/

# === Certificate locale (https dev) ===
*.pem
*.local.key
*.local.crt

# === OS / editor ===
.DS_Store
Thumbs.db
.idea/
*.swp

# === Tooling Claude Code (NU se comite — e setup local, nu cod de produs) ===
.claude/
.agents/
skills-lock.json

# === Continuitate sesiune (local, nu cod de proiect) ===
.remember/
/documente_client

# === CE SE COMITE (exemple, ca să fie clar pentru echipă) ===
# docs/                       -> documentația (ARHITECTURA.md, CHANGELOG.md, etc.)
# CLAUDE.md                   -> specificația de domeniu a proiectului
# .env.example                -> template de env, fără valori reale
